# Environment Configuration
# Options: Development, Production, Staging
ENVIRONMENT=Development

# Backend Service (.NET API) Configuration
# Internal port - not exposed to host, only accessible via Docker network
SERVICE_INTERNAL_PORT=5000

# Frontend UI (Angular/Nginx) Configuration
# Internal port - not exposed to host, only accessible via Docker network
UI_INTERNAL_PORT=80

# API Gateway (Nginx) Configuration
# This will be the ONLY entry point exposed to the host
# Handles: routing, basic auth, rate limiting, SSL termination
# API_INTERNAL_PORT=80
# API_EXTERNAL_PORT=8080

# Docker Configuration
# Options: no, always, on-failure, unless-stopped
RESTART_POLICY=no
